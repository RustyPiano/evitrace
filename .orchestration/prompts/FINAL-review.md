# 最终整体审核：EviTrace 全项目（交付前）

你是独立终审员（只读，禁止修改）。这是全项目交付前的最终审核，重点是**跨里程碑一致性、整体 SPEC 验收、是否有遗漏的 MUST 或越界的禁止项、端到端正确性与安全**。各里程碑此前已分别审核并修复；现在请站在整体视角找仍存在的真实问题。

## 依据
- `SPEC.md`（全文，尤其 §2 范围、§4.2 状态机、§13 错误码、§15 验收标准、§17 实现决策）、`PLAN.md`（第 9 章测试矩阵、第 11 章禁止事项、第 13 章交付清单）。
- 全代码：`backend/app/**`、`frontend/src/**`、`scripts/**`、`docker-compose.yml`、`Dockerfile`、`README.md`、`demo_data/**`、`evaluation_result.md`。

## 终审重点
1. **SPEC §15.1 功能验收逐条**：登录/权限隔离、各格式上传与拒绝、PDF 页码、图片 OCR 区域、音频/视频时间跳转证据、运行状态进度、实体/时间线/冲突、三类冲突各有自动化测试、报告引用有效、点击引用打开证据、下载 md、admin 启停非必需 Skill、Docker 可启动——逐条给出「满足/部分/缺失 + 证据」。
2. **SPEC §15.2 质量验收**：coverage≥0.9、invalid==0、植入冲突≥80%、所有模型输出过 Pydantic/JSON 校验、单文件失败不致全任务无果、关键操作有审计、MOCK 端到端通过——逐条核验。
3. **禁止项/范围**（SPEC §2.2、PLAN §11）：是否引入向量库/图谱/LangChain/聊天主页/互联网搜索/自动代码执行/实时流；是否有后端权限被前端绕过；报告是否出现无证据编号的确定性事实；是否伪造证据。
4. **跨里程碑一致性**：状态枚举、错误码、API 路径、证据/事件/冲突结构在前后端与各模块间是否一致；M2 的 parse_all_files 与 M3 orchestrator 复用是否真正干净；evidence display_id 与引用联动（含 >50 条）是否端到端正确。
5. **安全整体**：JWT、IDOR（所有资源继承任务归属）、文件类型/路径穿越、报告 Markdown XSS、健康/审计脱敏、密钥与离线、容器加固；有无任何回归。
6. **可恢复/单运行/不挂死**：orchestrator 异常路径、启动恢复、单机单运行、ffmpeg 超时、后台任务异常兜底。
7. **交付清单（PLAN §13）**：是否齐全（SPEC/PLAN/源码/.env.example/compose/后端测试/前端构建/3 组演示/evaluation_result.md/README）。
8. **回归风险**：后续里程碑是否破坏了早期修复（如路径锚定、CORS、错误外泄、3.11 注解、审计 append-only、完成门禁）。

## 输出（Markdown）
- 先给 §15.1 与 §15.2 的逐条核验表（满足/部分/缺失 + 证据文件:行）。
- 再列剩余**真实问题**：严重级别（BLOCKER/MAJOR/MINOR/NIT）、文件:行、问题、修复建议、置信度。
- 最后总评：是否达到 MVP 交付标准（SHIP / SHIP-WITH-FIXES / NO-SHIP）+ 交付前必修项清单。
只报告真实问题，重点是跨里程碑/整体层面之前可能漏掉的，不要复述已修复项。
